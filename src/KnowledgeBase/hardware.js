'use strict';

module.exports = {
  lector:
    'Si el lector no enciende, verifica la base de carga, el cable USB y el nivel de batería. Si continúa sin responder, registra el modelo y el comportamiento observado.',
  wifi:
    'Si el handheld no se conecta al WiFi, confirma que la red esté disponible, valida la contraseña y reinicia la conexión inalámbrica del dispositivo.',
  bateria:
    'Carga completamente la batería y prueba nuevamente. Si la autonomía sigue siendo anormal, la batería puede requerir reemplazo.',
};
